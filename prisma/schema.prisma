generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================
// NOTE: MedSoft Integration
// ============================================
// The booking system now uses MedSoft CLOUD API
// for all appointment and patient management.
//
// Previous models (Doctor, Service, Booking, Notification)
// have been removed as they are managed by MedSoft.
//
// This schema is kept minimal. Add models below
// if you need local data storage for other features.
// ============================================
